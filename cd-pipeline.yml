# name: CD

# on:
#   workflow_run:
#     workflows: ["CI"]
#     types: [completed]

# permissions:
#   contents: read
#   packages: write

# env:
#   REGISTRY: ghcr.io

# jobs:
#   build-and-push-frontend:
#     runs-on: ubuntu-latest
#     timeout-minutes: 30
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v6

#       - name: Set image variables
#         run: echo "IMAGE=${{ env.REGISTRY }}/${{ github.repository_owner }}/diagram_frontend" >> $GITHUB_ENV

#       - name: Set up QEMU (for arm/amd64 cross-builds)
#         uses: docker/setup-qemu-action@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v4

#       - name: Log in to registry (GHCR)
#         uses: docker/login-action@v4
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Create .trivyignore file
#         run: |
#           echo 'CVE-2026-40200' > .trivyignore # musl lib
#           echo 'CVE-2026-22184' >> .trivyignore # z lib
#           echo 'CVE-2026-33671' >> .trivyignore # picomatch
#           echo 'CVE-2026-27135' >> .trivyignore # nghttp2

#       - name: Build local image for smoke test (single-arch, load into daemon)
#         uses: docker/build-push-action@v4
#         with:
#           context: ./frontend
#           file: ./frontend/Dockerfile
#           load: true
#           push: false
#           tags: ${{ env.IMAGE }}:${{ github.sha }}
#           no-cache: true

#       - name: Scan image with Trivy (fail on HIGH/CRITICAL)
#         run: |
#           docker run --rm \
#             -v /var/run/docker.sock:/var/run/docker.sock \
#             -v ${{ github.workspace }}/.trivyignore:/.trivyignore \
#             aquasec/trivy:0.41.0 image --severity HIGH,CRITICAL --exit-code 1 ${{ env.IMAGE }}:${{ github.sha }}

#       - name: Run frontend smoke container
#         run: |
#           docker run -d --name smoke-frontend-${{ github.run_id }} -p 127.0.0.1:8080:8080 ${{ env.IMAGE }}:${{ github.sha }}

#       - name: Wait for frontend
#         run: |
#           for i in $(seq 1 30); do
#             if curl -fsS http://127.0.0.1:8080/ >/dev/null; then
#               echo "Frontend up"; exit 0
#             fi
#             sleep 1
#           done
#           echo "Frontend did not respond"; docker logs smoke-frontend-${{ github.run_id }} || true; exit 1

#       - name: Frontend smoke test
#         run: curl -fsS http://127.0.0.1:8080/ >/dev/null

#       - name: Push multi-arch image to registry (amd64 + arm64)
#         uses: docker/build-push-action@v4
#         with:
#           context: ./frontend
#           file: ./frontend/Dockerfile
#           push: true
#           platforms: linux/amd64,linux/arm64
#           tags: |
#             ${{ env.IMAGE }}:${{ github.sha }}
#             ${{ env.IMAGE }}:latest
#           no-cache: true

#       - name: Cleanup frontend container and local image
#         run: |
#           docker rm -f smoke-frontend-${{ github.run_id }} || true
#           docker rmi ${{ env.IMAGE }}:${{ github.sha }} || true

#   build-and-push-backend:
#     runs-on: ubuntu-latest
#     timeout-minutes: 30
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v6

#       - name: Set image variables
#         run: echo "IMAGE=${{ env.REGISTRY }}/${{ github.repository_owner }}/diagram_backend" >> $GITHUB_ENV

#       - name: Set up QEMU (for arm/amd64 cross-builds)
#         uses: docker/setup-qemu-action@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v4

#       - name: Log in to registry (GHCR)
#         uses: docker/login-action@v4
#         with:
#           registry: ${{ env.REGISTRY }}
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Build local image for smoke test (single-arch, load into daemon)
#         uses: docker/build-push-action@v4
#         with:
#           context: ./backend
#           file: ./backend/Dockerfile
#           load: true
#           push: false
#           tags: ${{ env.IMAGE }}:${{ github.sha }}
#           no-cache: true

#       - name: Create .trivyignore file
#         run: |
#           echo 'CVE-2026-33671' >> .trivyignore # picomatch vulnerability

#       - name: Scan image with Trivy (fail on HIGH/CRITICAL)
#         run: |
#           docker run --rm \
#             -v /var/run/docker.sock:/var/run/docker.sock \
#             -v ${{ github.workspace }}/.trivyignore:/.trivyignore \
#             aquasec/trivy:0.41.0 image --severity HIGH,CRITICAL --exit-code 1 ${{ env.IMAGE }}:${{ github.sha }}

#       - name: Run backend smoke container
#         run: |
#           docker run -d --name smoke-backend-${{ github.run_id }} -p 127.0.0.1:5000:5000 ${{ env.IMAGE }}:${{ github.sha }}

#       - name: Wait for backend
#         run: |
#           for i in $(seq 1 30); do
#             if curl -fsS http://127.0.0.1:5000/api/health >/dev/null; then
#               echo "Backend up"; exit 0
#             fi
#             sleep 1
#           done
#           echo "Backend did not respond"; docker logs smoke-backend-${{ github.run_id }} || true; exit 1

#       - name: Backend smoke test
#         run: curl -fsS http://127.0.0.1:5000/api/health >/dev/null

#       - name: Push multi-arch image to registry (amd64 + arm64)
#         uses: docker/build-push-action@v4
#         with:
#           context: ./backend
#           file: ./backend/Dockerfile
#           push: true
#           platforms: linux/amd64,linux/arm64
#           tags: |
#             ${{ env.IMAGE }}:${{ github.sha }}
#             ${{ env.IMAGE }}:latest
#           no-cache: true

#       - name: Cleanup backend container and local image
#         run: |
#           docker rm -f smoke-backend-${{ github.run_id }} || true
#           docker rmi ${{ env.IMAGE }}:${{ github.sha }} || true
