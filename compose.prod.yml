services:
  frontend:
    build:
      context: ./frontend
      target: prod
    container_name: diagram_frontend_prod
    depends_on:
      - backend
    restart: unless-stopped
  backend:
    build:
      context: ./backend
      target: prod
    container_name: diagram_backend_prod
