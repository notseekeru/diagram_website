VITE_BACKEND_URL=

# Vite dev proxy target (only used in dev mode via volume mount)
DEVELOPMENT_BACKEND_URL=http://backend:5050

# Required for Docker file watcher in containers
CHOKIDAR_USEPOLLING=true
WATCHPACK_POLLING=true
