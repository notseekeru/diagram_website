# Trivy ignore policy (v0.56+ / .trivyignore.yaml)
#
# Empty by design: the shipping artifact is an nginx-unprivileged Debian
# image serving a static bundle, which currently scans 0 HIGH/CRITICAL
# findings (last verified against :1.31.4). Prior alpine apk/* OS purls and
# never-shipped npm devDeps no longer apply.
#
# Dated, scoped exceptions with reachability rationale live here. When
# `expired_at` passes, the CD/`make prod-scan` gate re-fails until each is
# re-verified (fixed by base bump / dep upgrade, or explicitly re-accepted).
# Keep entries short-lived; prefer a base bump over an exception.

vulnerabilities: []
